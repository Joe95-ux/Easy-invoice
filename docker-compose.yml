services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: easyinvoice
      POSTGRES_PASSWORD: easyinvoice
      POSTGRES_DB: easyinvoice
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
